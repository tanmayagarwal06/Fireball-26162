# ML & geospatial pipeline dependencies (SIH26162). Install with:
#   python -m pip install -r requirements-pipeline.txt
# Tested on Python 3.14 / Windows 11 with the versions pinned below.
numpy>=2.0
pandas>=2.2
pyarrow>=15
scipy>=1.12
scikit-learn>=1.4
xgboost>=2.1          # CUDA-enabled wheel; falls back to CPU automatically
lightgbm>=4.3
geopandas>=1.0
shapely>=2.0
pyogrio>=0.9
pyproj>=3.6
rasterio>=1.3         # only used when ESA WorldCover tiles are present in data/cache/worldcover
